#include <vector>

double dot_product(std::vector<double> a, std::vector<double> b);
std::vector<double> scale_vector(std::vector<double> vec, double scalar);
std::vector<double> add_vector(std::vector<double> a, std::vector<double> b);

/*
 * int dot_product(std::vector<int> a, std::vector<int> b)
 * std::vector<int> scale_vector(std::vector<int> vec, int scalar);
 * std::vector<int> add_vector(std::vector<int> a, std::vector<int> b);
*/
